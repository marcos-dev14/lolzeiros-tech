import styled from 'styled-components';
import { BoxBadge } from '../../styles/components/box';

export const Badge = styled(BoxBadge)`
  padding: 0 0.25rem;
  min-width: 2rem;
  font-size: 0.75rem;
`;